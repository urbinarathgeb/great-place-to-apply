---
export interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class={`glass shadow-float rounded-2xl text-card-foreground ${className ?? ''}`}>
  <slot />
</div>
